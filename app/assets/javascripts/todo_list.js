// This file is, by convention, called app.js.coffee; we decided to call it something else instead.

// This module is to declare all the submodules that will be used throughout the app
var todo = angular.module('todo', [

]);